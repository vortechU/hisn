<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Used to calculate prayer times for the user's location. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Reverse-geocoding the detected location into a town name. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Prayer-time reminders (scheduled local notifications). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Play the full adhan reliably at prayer time via a foreground service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="Hisn"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: deliver scheduled notifications and
             re-register them after a reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Adhan playback: an exact alarm fires this receiver, which starts the
             foreground service that plays the call to prayer. -->
        <receiver
            android:name=".AdhanAlarmReceiver"
            android:exported="false" />
        <!-- Re-arm the adhan alarms after a reboot or app update (AlarmManager
             drops them on reboot). -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <service
            android:name=".AdhanPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Hands-free adhkar. Unlike the adhan above — a one-shot with no
             transport — a recited set is a real media session: the lock
             screen, headset buttons, Bluetooth and a car head unit all drive
             it, and the foreground service is what keeps the process (and so
             the counter that advances with the audio) alive with the screen
             off. Supplied by audio_service; the two players are independent
             and the OS arbitrates focus between them. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <!-- Routes headset / steering-wheel media buttons into that session. -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Home-screen widget showing the five daily prayer times. Times are
             computed natively (Adhan), so it stays accurate while the app is
             closed; Flutter pushes the location + settings via the hisn/widget
             channel. -->
        <receiver
            android:name=".PrayerWidgetProvider"
            android:exported="false"
            android:label="@string/widget_prayer_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>
        <!-- Compact variant: next prayer + live countdown. -->
        <receiver
            android:name=".PrayerWidgetSmallProvider"
            android:exported="false"
            android:label="@string/widget_prayer_small_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_small_info" />
        </receiver>

        <!-- The tasbih counter. Alone among the widgets this one writes: its
             taps land in the app's own preference store so the home screen and
             the Tasbih screen keep a single count (see FlutterPrefs). The
             counting broadcast is an explicit intent back to this receiver,
             which needs no intent-filter and no export. -->
        <receiver
            android:name=".TasbihWidgetProvider"
            android:exported="false"
            android:label="@string/widget_tasbih_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tasbih_widget_info" />
        </receiver>

        <!-- The adhkar set belonging to this part of the day. Which set that is
             is recomputed natively from the prayer times on every redraw, so it
             turns over without the app being opened. -->
        <receiver
            android:name=".AdhkarWidgetProvider"
            android:exported="false"
            android:label="@string/widget_adhkar_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/adhkar_widget_info" />
        </receiver>

        <!-- A verse or supplication, picked from a pushed pool by the date. -->
        <receiver
            android:name=".AyahWidgetProvider"
            android:exported="false"
            android:label="@string/widget_ayah_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ayah_widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
